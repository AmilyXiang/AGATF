[
  {
    "id": "TC_CALL_001",
    "name": "basic_call",
    "description": "Place a basic call and end it cleanly.",
    "scope": "common",
    "required_capabilities": ["basic_call"],
    "required_dut_roles": ["caller", "callee"],
    "steps": [
      {"action": "dial", "expected": "connected", "timeout": 30},
      {"action": "answer", "expected": "connected", "timeout": 30}
    ]
  },
  {
    "id": "TC_HOLD_001",
    "name": "hold_resume",
    "description": "Hold and resume a call.",
    "scope": "common",
    "required_capabilities": ["hold", "resume"],
    "required_dut_roles": ["caller", "callee"],
    "steps": [
      {"action": "dial", "expected": "connected", "timeout": 30},
      {"action": "hold", "expected": "hold", "timeout": 30},
      {"action": "resume", "expected": "connected", "timeout": 30}
    ]
  },
  {
    "id": "TC_AUDIO_001",
    "name": "audio_check",
    "description": "Verify two-way audio using the lab audio resource.",
    "scope": "common",
    "required_capabilities": ["basic_call", "audio"],
    "required_dut_roles": ["caller", "callee"],
    "required_resources": ["audio"],
    "steps": [
      {"action": "dial", "expected": "connected", "timeout": 30},
      {"action": "answer", "expected": "connected", "timeout": 30}
    ]
  }
]

